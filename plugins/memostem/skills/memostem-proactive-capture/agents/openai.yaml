interface:
  display_name: MemoStem Proactive Capture
  short_description: Spot useful knowledge, ask once, then create private review drafts.
  brand_color: "#21124D"
  default_prompt: Notice one independently teachable idea in this conversation and ask whether I want to keep that specific idea. Only after I confirm, verify MemoStem OAuth and create a private pending atomic memo or question-and-answer draft, then report the real review link.
policy:
  allow_implicit_invocation: true
