{
  "name": "memostem-plugins",
  "version": "0.5.2",
  "private": true,
  "description": "Suggest reusable AI-conversation knowledge and create private MemoStem drafts only after consent.",
  "scripts": {
    "check": "node scripts/validate.mjs && node --test scripts/validate.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
