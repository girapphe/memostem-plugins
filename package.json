{
  "name": "memostem-plugins",
  "version": "0.6.2",
  "private": true,
  "description": "Your reviewed knowledge, across AI conversations.",
  "scripts": {
    "check": "node scripts/validate.mjs && node --test scripts/*.test.mjs",
    "check:submission": "node scripts/check-submission-readiness.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
