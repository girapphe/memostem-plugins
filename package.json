{
  "name": "memostem-plugins",
  "version": "0.5.4",
  "private": true,
  "description": "Reuse owner-scoped private knowledge and create reviewable MemoStem drafts after consent.",
  "scripts": {
    "check": "node scripts/validate.mjs && node --test scripts/validate.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
