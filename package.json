{
  "name": "memostem-plugins",
  "version": "0.5.3",
  "private": true,
  "description": "Find reusable knowledge during AI conversations and create private MemoStem drafts only after consent.",
  "scripts": {
    "check": "node scripts/validate.mjs && node --test scripts/validate.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
